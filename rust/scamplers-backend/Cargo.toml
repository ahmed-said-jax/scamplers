[package]
name = "scamplers-backend"
version = "0.1.0"
edition.workspace = true

[dependencies]
scamplers-core = { path = "../scamplers-core", features = ["backend"] }
scamplers-schema = { path = "../scamplers-schema" }

[lints]
workspace = true
