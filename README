# Scamplers

```
Usage: scamplers <COMMAND>

Commands:
  sync-files          TODO
  sync-google-sheets  TODO
  sync-nf-tenx        TODO
  help                Print this message or the help of the given subcommand(s)

Options:
  -h, --help     Print help
  -V, --version  Print version
```
When connected to a MongoDB, this command-line utility can ingest `data_set`s and `lab`s via JSON files, and overwrite those outputs with data from a run of [nf-tenx](https://github.com/TheJacksonLaboratory/nf-tenx). This behavior will of course be corrected.